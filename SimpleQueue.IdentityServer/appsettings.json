{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "mySqlConfigConnection": "server=localhost;database=YourConfigDatabaseName;user=YourUser(root by default);password=YourConnectionPassword",
    "mySqlConnection": "server=localhost;database=YourDatabaseName;user=YourUser(root by default);password=YourConnectionPassword"
  }
}
